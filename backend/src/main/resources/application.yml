server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tablename?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: RootPasswort0815
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        storage_engine: innodb
  jackson:
    serialization:
      write-dates-as-timestamps: false

app:
  cors:
    allowed-origins: http://localhost:9002,http://localhost:3000
